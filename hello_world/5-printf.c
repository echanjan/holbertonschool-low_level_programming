#include <stdio.h>
/**
 * main - imprime usando funcion printf
 * no esta permitido usar funcion puts
 * return (0)
 * el programa debe compilarse sin previo aviso al usar la opción -Wall gcc
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
